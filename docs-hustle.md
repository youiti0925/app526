# 副業パイプライン（/hustle）

同じアプリの中に同居している、もうひとつのツール。
**「稼ぐための作業」を自動化して、最初の1円までの距離を縮めるためのもの**です。

---

## 最初に断っておくこと

**完全に自動でお金が入ってくるアプリは作れません。**
そう宣伝されているものは、ほぼ例外なく「それを売る側だけが儲かる」構造です。
お金がないときにそこへ払うのが、いちばん損失の大きい選択になります。

このツールが自動化するのは次のような、毎回ほぼ同じ形をしている作業です。

- 案件が受けていいものかを判定する
- 提案文・営業メール・出品文を書く
- 納品物の初稿を作る
- 手数料を引いた実際の手取りと実効時給を計算する

自動化していないのは、相手との信用をつくる部分です。そこだけに時間を使えるようにするのが目的です。

---

## 機能

| 画面 | やること |
|---|---|
| `/hustle` | 今日やることのキュー。30日プランから自動生成される。完了すると作業時間が自動で記録される |
| `/hustle/diagnose` | 時間・資金・スキル・期限を入れて、入金の速さ順に副業を並べ替える。元手が足りないものは除外する |
| `/hustle/proposal` | 案件を貼ると、詐欺判定・想定作業時間・実効時給・範囲が膨らむ箇所を出し、通れば提案文を3案つくる |
| `/hustle/batch` | 案件をまとめて貼ると、実効時給の見込み順に並べ替える。選ぶ時間をゼロにする |
| `/hustle/factory` | 提案文・営業メール・出品文・プロフィール・納品物の初稿など8種類のテンプレートから3案生成 |
| `/hustle/money` | 収支と投入時間から実効時給を出し、撤退すべきチャネルを判定する。手数料込みの手取り計算つき |
| `/hustle/scam` | 募集文・DMを貼ると、法令と消費者被害の典型手口に照らして危険度を判定する |
| `/hustle/guide` | 調べた結果の現実と、副業より先に使うべき公的支援・相談窓口 |

### 意図的に作らなかったもの

| 作らなかったもの | 理由 |
|---|---|
| 自動応募・自動営業 | 各プラットフォームの規約でBAN対象 |
| 検索結果の自動収集 | Google の利用規約と robots.txt に反する |
| AI記事の自動投稿 | Googleのスパムポリシーとブログサービスの規約に抵触する |
| 収益の予測グラフ | 当たらないうえ、当たると信じさせること自体が有害 |
| 有料プラン | 稼げていない人から金を取る構造を作らないため |

---

## 使い方

### 1. 起動

```bash
npm install
npm run dev
# http://localhost:3000/hustle
```

### 2. AIキーの設定（任意）

`/settings` で Google AI Studio の **無料** APIキーを登録すると、生成機能がAIで動きます。

**キーが無くてもアプリは全機能が動きます。**
生成はテンプレートの穴埋め雛形になり、詐欺判定・診断・収支計算・手取り計算はもともとAIを使っていません。
無料枠の上限に当たった場合も、雛形にフォールバックして手が止まらないようにしてあります。

環境変数 `GEMINI_API_KEY` を設定した場合はそちらが優先されます。

> **無料枠に入れてはいけないもの**
> 生成AIの無料枠は、入力と出力が品質改善に使われ、人が読む場合があると規約に明記されています。
> 顧客の会議音声・社内文書・個人情報など、外に出せない情報は絶対に貼らないでください。
> 規約違反であると同時に、顧客に対する守秘義務違反になります。
> 機密を扱う仕事を受ける段階になったら、入力を学習に使わない有料プランか、
> ローカル実行（whisper.cpp など）に切り替えてください。
>
> また、無料枠の上限は保証されていません。納期のある仕事のクリティカルパスに置かず、
> このアプリのように「使えなければ雛形で進む」形にしておいてください。

### 3. 流れ

1. `/hustle/diagnose` で条件を入れて診断する → 選んだチャネルの30日ぶんのタスクが入る
2. `/hustle` のキューを上から潰す
3. 案件を見つけたら、応募する前に `/hustle/proposal` に通す
4. 作業したら `/hustle/money` に時間を記録する（提案文を書いた時間も含める）
5. 入金されたら記録する。実効時給が最低賃金を割っていたら、アプリが撤退を勧める

---

## データの保存

- サーバー側は SQLite（`data/videosop.db`）に保存されます
- 同時に、全データをブラウザの localStorage にもミラーしています
- Vercel のようにサーバーのファイルシステムが揮発する環境では、サーバーが空になった時点で
  ブラウザのミラーから自動で書き戻します
- 画面右上から JSON でのバックアップ書き出し・読み込みができます

環境変数 `APP_DATA_DIR` でデータの保存先を変更できます。

---

## テスト

判断に直結するロジック（詐欺スコアリング、実効時給、手取り計算、診断、データ整合性）には
回帰テストがあります。

```bash
npm test        # tsc でロジックだけ切り出してコンパイル → node:test
npm run typecheck
npm run build
```

---

## 調べた結果の要約

8カテゴリを個別に調査し、収益の主張は別のエージェントに反証させ、
自動化の実現性は公式ドキュメントに当たって検証しました。結論は厳しいものでした。

| チャネル | 判定 | 最初の入金まで（中央値） | 3か月目の月収（中央値） |
|---|---|---|---|
| 不用品を売る | つなぎとしてなら | 10日（実質7〜14日） | 0〜5,000円・2〜3か月で枯渇 |
| クラウドソーシング受託 | 条件付き | 30日（現実的に3〜6週間） | 3,000〜10,000円（続いた人の中央値） |
| 地元事業者へのAI業務代行 | 条件付き | 90日（人脈なしなら3〜6か月） | 0〜30,000円 |
| 個人開発・ツール販売 | 非推奨 | 90日 | 0円 |
| ブログ・SEOアフィリエイト | 非推奨 | 180日 | 0円 |
| デジタル素材・スタンプ販売 | 非推奨 | 150日 | 0円（販路が閉じつつある） |
| 動画の収益化 | 最も非推奨 | 300日 | 0円 |

「AIで量産して稼ぐ」という戦略は、2025〜2026年のあいだに主要プラットフォームが
軒並み塞ぎました。Googleのスパムポリシー、YouTubeの収益化ポリシー、
ストックサービスのAI受付停止、フリマの画像ルール。このアプリが
自動投稿・自動応募・自動出品を実装していないのは、単に動かないからです。

## 数字の出どころ

各チャネルの「最初の入金までの日数」「3ヶ月目の月収」などは、
公的統計・各プラットフォームの公式開示・決算資料から算出した**中央値**です。
成功者の数字は載せていません。根拠は診断結果の「この数字の根拠」に表示されます。

主な参照元:

- 各クラウドソーシング事業者の手数料表・出金条件（公式ヘルプ）および決算説明資料
- アフィリエイト関連団体の意識調査（収入分布）
- Google 検索スパムポリシー / YouTube 収益化ポリシー
- 各ブログ・ストックサービスの利用規約およびAI生成物ポリシー
- 消費者庁・国民生活センター・警察庁の副業関連の注意喚起
- 特定商取引法、景品表示法（ステマ規制）、特定電子メール法、古物営業法、犯罪収益移転防止法

規約も相場も変わります。金額が大きくなってきたら、必ず一次情報を確認してください。

---

## 困っているとき

副業で最初の1円が口座に入るまで、どんなに速くても2〜3週間かかります。
家賃や光熱費の期限がそれより早いなら、副業では間に合いません。

- 消費者ホットライン **188**（局番なし）— 詐欺・契約トラブル
- 警察相談専用電話 **#9110** — 闇バイト、口座の譲渡、脅迫
- お住まいの市区町村の**自立相談支援機関** — 家賃・生活費・就労の相談（無料）

詳しくは `/hustle/guide` に載せてあります。
